[project]
name = "springtorch-jobs"
version = "0.1.0"
description = "Temporal workers and jobs for SpringTorch"
requires-python = ">=3.14"
dependencies = [
    "temporalio",
]

[dependency-groups]
dev = []
